
docker build -t my-ibmmq-arm .

docker images


docker run --rm -it my-ibmmq-arm

podman build --build-arg HTTP_PROXY=http://proxy.example.com:port --build-arg HTTPS_PROXY=http://proxy.example.com:port --build-arg NO_PROXY=localhost,127.0.0.1,.example.com -t my-image .